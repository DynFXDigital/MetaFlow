---
description: "Rules for writing validation procedures (VTP IDs) that execute validation test cases."
applyTo: "doc/validation/**/ftd/**/*.md"
---

# Validation FTD authoring rules

- Use stable `VTP-####` IDs.
- Every `VTC-*` must be covered by at least one `VTP-*`.
- Include prerequisites and environment notes.
- Include a traceability matrix (VTP → VTC).
