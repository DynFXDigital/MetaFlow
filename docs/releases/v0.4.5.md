# MetaFlow 0.4.5

This patch release makes plugin metadata maintenance stable and keeps marketplace documentation out of capability discovery.

## Fixes

- Keep marketplace-level README files as documentation instead of toggleable capabilities.
- Produce deterministic `plugin.json` and `marketplace.json` output, reducing source-control churn during metadata maintenance.
