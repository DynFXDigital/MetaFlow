# MetaFlow 0.4.4

This stable point release keeps marketplace-level README files as documentation instead of exposing them as toggleable capabilities.

### Fixed

- Excluded marketplace repository roots from capability discovery and metadata loading, including stale restored catalog entries.
