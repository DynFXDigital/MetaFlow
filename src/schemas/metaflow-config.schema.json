{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MetaFlow Configuration",
  "type": "object",
  "description": "MetaFlow config for AI metadata repository sources, profile capability selections, and injection behavior.",
  "additionalProperties": false,
  "properties": {
    "compatibilityVersion": {
      "type": "integer",
      "minimum": 1,
      "description": "Config compatibility version used by MetaFlow for release-aware automatic migration."
    },
    "metadataRepos": {
      "type": "array",
      "description": "Public authored metadata repository descriptors. Capability activation is owned by profiles.",
      "items": {
        "$ref": "#/definitions/namedMetadataRepo"
      }
    },
    "metadataRepo": {
      "$ref": "#/definitions/metadataRepo",
      "description": "Legacy single-repo compatibility field. MetaFlow migrates this to metadataRepos[*].capabilities on load."
    },
    "layers": {
      "type": "array",
      "description": "Legacy single-repo compatibility field. MetaFlow migrates this to metadataRepos[*].capabilities on load.",
      "items": {
        "type": "string"
      }
    },
    "layerSources": {
      "type": "array",
      "description": "Legacy/internal compatibility field. MetaFlow migrates this to metadataRepos[*].capabilities on load.",
      "items": {
        "$ref": "#/definitions/layerSource"
      }
    },
    "profiles": {
      "type": "object",
      "description": "Named activation profiles.",
      "additionalProperties": {
        "$ref": "#/definitions/profileConfig"
      }
    },
    "activeProfile": {
      "type": "string",
      "description": "Currently active profile name."
    },
    "capabilityOverrides": {
      "type": "object",
      "description": "Sparse capability settings keyed by repo-qualified capability reference.",
      "additionalProperties": {
        "$ref": "#/definitions/capabilityOverride"
      }
    },
    "injection": {
      "$ref": "#/definitions/injectionConfig"
    },
    "settingsInjectionTarget": {
      "type": "string",
      "enum": ["user", "workspace", "workspaceFolder"],
      "default": "workspace",
      "description": "Default VS Code configuration scope for settings-backed injection. Can be overridden locally via the metaflow.injection.target VS Code setting."
    },
    "hooks": {
      "$ref": "#/definitions/hooksConfig"
    }
  },
  "anyOf": [
    {
      "required": ["metadataRepos"]
    },
    {
      "required": ["metadataRepo"]
    }
  ],
  "definitions": {
    "metadataRepo": {
      "type": "object",
      "additionalProperties": false,
      "required": ["localPath"],
      "properties": {
        "name": {
          "type": "string"
        },
        "url": {
          "type": "string"
        },
        "localPath": {
          "type": "string",
          "description": "Absolute path or workspace-relative path to the metadata repository clone."
        },
        "commit": {
          "type": "string",
          "description": "Optional pinned commit SHA for determinism."
        }
      }
    },
    "repoDiscoveryConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "exclude": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "capabilitySource": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path"],
      "properties": {
        "path": {
          "type": "string",
          "description": "Path within the metadata repository, such as company/core."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether this capability is enabled. Defaults to true when omitted."
        },
        "injection": {
          "$ref": "#/definitions/injectionConfig",
          "description": "Capability-scoped injection mode overrides. Overrides repo-level and top-level defaults."
        }
      }
    },
    "namedMetadataRepo": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "localPath"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for this repository source."
        },
        "name": {
          "type": "string"
        },
        "url": {
          "type": "string"
        },
        "localPath": {
          "type": "string",
          "description": "Absolute path or workspace-relative path to the metadata repository clone."
        },
        "commit": {
          "type": "string",
          "description": "Optional pinned commit SHA for determinism."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether this repository source is enabled. Defaults to true when omitted."
        },
        "discover": {
          "$ref": "#/definitions/repoDiscoveryConfig"
        },
        "capabilities": {
          "type": "array",
          "description": "Legacy capability entries accepted only during migration; canonical files use profile enabledCapabilities references.",
          "items": {
            "$ref": "#/definitions/capabilitySource"
          }
        },
        "injection": {
          "$ref": "#/definitions/injectionConfig",
          "description": "Repo-scoped injection mode defaults. Overrides top-level defaults; overridden by capability-level."
        }
      }
    },
    "layerSource": {
      "type": "object",
      "additionalProperties": false,
      "required": ["repoId", "path"],
      "properties": {
        "repoId": {
          "type": "string"
        },
        "path": {
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        }
      }
    },
    "profileConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "displayName": {
          "type": "string"
        },
        "enabledCapabilities": {
          "type": "array",
          "description": "Complete repo-qualified capability references selected by this profile.",
          "items": {
            "type": "string",
            "pattern": "^[^:]+:.+$"
          },
          "uniqueItems": true
        },
        "enable": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "disable": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "layerOverrides": {
          "type": "array",
          "description": "Legacy per-layer overrides accepted only during migration.",
          "items": {
            "$ref": "#/definitions/profileLayerOverride"
          }
        }
      }
    },
    "profileLayerOverride": {
      "type": "object",
      "additionalProperties": false,
      "required": ["repoId", "path"],
      "properties": {
        "repoId": {
          "type": "string",
          "description": "Repo identifier from metadataRepos."
        },
        "path": {
          "type": "string",
          "description": "Layer path within the referenced repository."
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether this layer is enabled while the profile is active."
        },
        "excludedTypes": {
          "type": "array",
          "description": "Artifact-type directories to exclude for this layer while the profile is active.",
          "items": {
            "type": "string",
            "enum": ["instructions", "prompts", "agents", "skills"]
          }
        }
      }
    },
    "capabilityOverride": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "injection": {
          "$ref": "#/definitions/injectionConfig"
        },
        "fileNamingStrategy": {
          "type": "string",
          "enum": ["prefixed", "original-unless-conflict"]
        }
      }
    },
    "injectionConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "instructions": {
          "$ref": "#/definitions/injectionMode"
        },
        "prompts": {
          "$ref": "#/definitions/injectionMode"
        },
        "skills": {
          "$ref": "#/definitions/injectionMode"
        },
        "agents": {
          "$ref": "#/definitions/injectionMode"
        },
        "hooks": {
          "$ref": "#/definitions/injectionMode"
        },
        "chatmodes": {
          "$ref": "#/definitions/injectionMode"
        }
      }
    },
    "injectionMode": {
      "type": "string",
      "enum": ["settings", "synchronize", "plugin"]
    },
    "hooksConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "preApply": {
          "type": "string"
        },
        "postApply": {
          "type": "string"
        }
      }
    }
  }
}
