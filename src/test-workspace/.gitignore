# MetaFlow managed state fixture
.metaflow/*
!.metaflow/config.jsonc

# Generated built-in capability fixtures created by integration runs.
capabilities/
# MetaFlow managed state (choose one rule):
# Track .metaflow/config.jsonc; ignore .metaflow/state.json, or ignore .metaflow/ for local-only setup.
.metaflow/
