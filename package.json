{
  "name": "metaflow-workspace",
  "description": "MetaFlow workspace for layered AI metadata overlays for Copilot instructions, prompts, skills, and agents.",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "src"
  ],
  "scripts": {
    "build": "npm -w @metaflow/engine run build && npm -w @metaflow/cli run build && npm -w metaflow-ai run compile",
    "format": "prettier . --write --cache && npm -w metaflow-ai run lint -- --fix",
    "format:check": "prettier . --check && npm -w metaflow-ai run lint",
    "gate:quick": "npm run build && npm -w metaflow-ai run lint && npm run test",
    "gate:integration": "npm -w metaflow-ai run gate:integration",
    "gate:full": "npm run test:engine && npm run test:cli && npm -w metaflow-ai run gate:quick && npm -w metaflow-ai run gate:integration",
    "test:engine": "npm -w @metaflow/engine test",
    "test:cli": "npm -w @metaflow/cli test",
    "test:unit": "npm -w metaflow-ai run test:unit",
    "test:integration": "npm -w metaflow-ai run test:integration",
    "test": "npm run test:engine && npm run test:cli && npm run test:unit",
    "changeset": "changeset",
    "version-packages": "changeset version && npm install --package-lock-only"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.6",
    "prettier": "^3.8.1"
  }
}
